@model IEnumerable<Negarandeh.Models.Section>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Season.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title_EN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title_FA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Index)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Season.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title_EN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title_FA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Index)
        </td>
        <td>
            @Html.ActionLink("Seasons", "Index", new { id = item.Id, controller = "Seasons" }) |
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
